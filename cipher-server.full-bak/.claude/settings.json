{
  "permissions": {
    "allow": [
      "Bash(sudo systemctl restart cipher*)",
      "Bash(sudo systemctl status cipher*)",
      "Bash(sudo systemctl stop cipher*)",
      "Bash(sudo journalctl -u cipher*)",
      "Bash(sudo journalctl -u cipher -f*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/home/Szymon/Cipher/cipher-server/.claude/scripts/save-session.sh",
            "async": true
          }
        ]
      }
    ]
  }
}
