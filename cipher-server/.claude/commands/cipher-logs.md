Mostra le ultime 50 righe di log di Cipher.

Esegui: `sudo journalctl -u cipher -n 50 --no-pager`

Se l'utente specifica un numero diverso di righe o un servizio diverso (cipher-telegram, cipher-memory), adatta il comando di conseguenza.
